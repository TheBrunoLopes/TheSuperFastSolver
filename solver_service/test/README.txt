You can run the tests present on this package by running
$ pytest
You can install pytest by doing
$ pip install pytest

This dependency is not in the requirements.txt file
because we don't need to increase the size of the docker image with test packages